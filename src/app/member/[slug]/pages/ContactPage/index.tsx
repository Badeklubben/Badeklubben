import { Member } from "@/common/sanityLoader";

export default function ContactPage({ 
    member
} : {
    member: Member
})   {
    return (
        <div>
        <h1>Eksempel på side</h1>
        </div>
    );
}